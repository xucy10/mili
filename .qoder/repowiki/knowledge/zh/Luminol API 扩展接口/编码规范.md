- 所有自定义事件均继承自 `org.bukkit.event.Event`，并采用静态 `HandlerList` 配合标准的 `getHandlers()` 与 `getHandlerList()` 方法实现事件注册。
- 事件类的构造函数普遍使用 `org.apache.commons.lang3.Validate.notNull` 对关键参数进行非空校验，确保运行时稳定性。
- 接口定义（如 `RegionStats`, `ThreadedRegion`）广泛使用 JavaDoc 标注线程上下文约束，明确方法调用的线程安全边界。