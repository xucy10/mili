- 通过 `moe.luminolmc.hyacinthusweight.patcher` 插件在根项目定义上游依赖（Luminol/Paper/Folia），并将补丁自动应用到 `lophine-api` 和 `lophine-server` 子模块。
- 采用多模块 Gradle 结构，`lophine-api` 负责定义 Bot 与回放录制的公共接口，`lophine-server` 负责底层协议实现与性能优化逻辑的落地。
- 在 `subprojects` 块中强制统一 Java 21 工具链、UTF-8 编码及 Maven 发布配置，确保跨模块开发环境的一致性。