- 广泛使用自定义注解（如 `@ProtocolHandler.PayloadReceiver`、`@ConfigInfo`）结合反射机制实现功能的自动注册与解耦。
- 在涉及实体操作的关键路径（如 `ServerBot.tick`）中强制插入 Folia 区域线程安全检查，确保跨区调度的安全性。
- 遵循配置与逻辑分离原则，所有可调优参数均封装在 `config.modules` 下的静态类中，并通过 `isActive()` 方法控制功能开关。