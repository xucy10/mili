- 采用 `moe.luminolmc.hyacinthusweight.patcher` 插件在根项目 orchestrator 中声明上游依赖（Luminol/Paper/Folia），并通过 `patchFile` 和 `patchRepo` 机制将变更同步至 `lophine-api` 与 `lophine-server` 子模块。
- 根 `build.gradle.kts` 统一定义 Java 21 工具链、UTF-8 编码规范及 Maven 发布仓库，确保所有子模块遵循一致的编译与分发契约。
- `settings.gradle.kts` 明确包含 `lophine-api` 与 `lophine-server`，形成“接口定义-核心实现”的双模块架构，通过 Gradle 依赖管理实现跨模块的功能引用。