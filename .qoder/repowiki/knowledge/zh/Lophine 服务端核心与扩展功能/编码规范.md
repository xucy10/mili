- 协议处理器类使用 @LeavesProtocol.Register 注解声明命名空间，并通过静态字段配合 @LeavesCustomPayload.ID 定义协议标识符。
- 机器人行为与配置采用注册中心模式，通过 Actions 和 Configs 类的静态初始化块集中注册具体实现类。
- 功能模块配置类实现 IConfigModule 接口，利用 @ConfigClassInfo 和 @ConfigInfo 注解定义配置项及其分类。
- 在 Folia 多线程环境下，关键实体逻辑（如 ServerBot.tick）包含线程归属检查，确保操作在正确的区域线程执行。